spring:
  application:
    name: spring-boot2-logging

# server
server:
  port: 8260

# logging
logging:
  level:
    root: info
    xyz.flysium: debug
  #  path: /opt/cache/webserver-logs
  config: classpath:logback-spring.xml

